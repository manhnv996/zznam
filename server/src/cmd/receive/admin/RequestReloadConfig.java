package cmd.receive.admin;

import bitzero.server.extensions.data.BaseCmd;
import bitzero.server.extensions.data.DataCmd;

public class RequestReloadConfig extends BaseCmd {
    public RequestReloadConfig(DataCmd dataCmd) {
        super(dataCmd);
    }
}
