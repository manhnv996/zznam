var NatureToolPopup = TablePopup.extend({
	popupItemList: null,

	ctor: function(lx, ly) {
		this.super(res.popup1, lx, ly, { width: 150, height: 180 });
		this.renderBomb();
	},

	renderBomb: function() {
		
	}
});
